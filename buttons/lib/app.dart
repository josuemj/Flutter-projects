import 'package:buttons/screens/buttons.dart';
import 'package:flutter/material.dart';

class ButtonsApp extends StatelessWidget {
  const ButtonsApp({super.key});

  @override
  Widget build(context) {
    return (Scaffold(
      body: ButtonsScreen(),
    ));
  }
}
